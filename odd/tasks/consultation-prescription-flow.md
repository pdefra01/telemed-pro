# Feature: Consultation prescription flow (diagnosis / receta / prescripcion + WhatsApp)

## Objective
Simplify how the doctor closes a consultation. Three separate things, all
reachable during the video call and finalized in PostConsultation:
- **Diagnostico**: unchanged (already required).
- **Receta**: the medications, printed in a plain PDF. Catalog picker (stock
  deduction kept) plus free-text lines that live together in the same
  `medications` array. NO digital signature anywhere (flow or PDF).
- **Prescripcion**: free-text non-medication indications (rest, diet, studies).
- Receta (PDF) and prescripcion (text) are both delivered to the affiliate by
  WhatsApp from the company number (WAHA, already linked and WORKING).
- **Obra social alternative**: for affiliates who need an insurer-issued
  prescription, the doctor generates it in an external phone app and pastes the
  download link; the system sends that link to the affiliate through the
  company WhatsApp instead of generating the internal PDF.

## Decisions (user-confirmed 2026-09-23)
- Free text COEXISTS with the catalog (catalog stays for stock deduction).
- Receta = medications in a PDF; "nothing signed or weird" -> remove the digital
  signature from the flow AND from the PDF.
- Prescripcion = non-medication indications, free text.
- Reconnect the existing but disconnected "Receta" tab in `VideoRoom.tsx`
  (`RxItem { med, dose }`, `recommendations`) instead of building new UI; its
  content must reach PostConsultation (navigate state) and be prefilled there.
- Obra social is an ALTERNATIVE mode at prescribing time (not both): doctor
  pastes the external app's download link; sent via company WhatsApp.

## Findings (mapping, 2026-09-23)
- `VideoRoom.tsx` ~L522-600: "Receta" tab exists (state at ~L876) but
  `handleCompleteAppointment` (~L1004-1016) only saves notes and navigates to
  `/doctor/post-consultation/:id`; prescription/recommendations are dropped.
- `PostConsultation.tsx`: `MedicationItem {name, instructions, productId?,
  stockAvailable?}`; payload to edge function `finalize-consultation`
  (`appointmentId, diagnosis, notes, medications, digitalSignature,
  signaturePublicKey`); stock deducted client-side after invoke when
  `productId` exists; WhatsApp send fires only if `data.pdfUrl` is returned.
- `supabase/functions/finalize-consultation/index.ts`: builds the PDF with
  pdf-lib, inserts `prescriptions` row (`medications` JSONB no strict shape,
  `notes` TEXT autogenerated "Recetado para: ...", `digital_signature TEXT NOT
  NULL` with an `AUTH-...` fallback).
- `server/whatsapp.js` `sendPrescriptionViaWhatsApp` sends the PDF via
  `sendFile`; `prescription_deliveries` needs a `prescription_id`.
- Root cause of the "receta not delivered" test on 2026-09-23: no `pdfUrl` was
  produced, so the WhatsApp endpoint was never called (`prescription_deliveries`
  was empty). The gateway itself was verified working (sendText + sendFile OK).

## Design (assistant)
- No DB migration by default (production DB, user asked for care):
  - Prescripcion is stored in the existing `prescriptions.notes` TEXT (replaces
    the autogenerated "Recetado para").
  - With the signature removed, `digital_signature` stays NOT NULL and the
    server stores an empty string (honest "no signature", satisfies the
    constraint).
- Receta PDF prints only medications (name + instructions) with no signature
  block. Prescripcion travels as a second WhatsApp text message after the PDF.
- Obra social mode: doctor selects mode, pastes an https link (validated), no
  PDF is generated; server sends a WhatsApp text with the link. Delivery log:
  `prescription_deliveries` requires a prescription id, so T5 must decide
  between creating a minimal `prescriptions` row (no PDF) or a written-only
  migration. Any migration is WRITTEN ONLY and applied only after explicit
  user confirmation.
- The edge function change requires a deploy to take effect in production:
  a separate user-authorized step, not part of the code tasks.

## Constraints
- TDD enabled (source: existing odd task files of this repo). Runner:
  `npx vitest run`. Edge function (Deno) test approach to be confirmed by the
  writer; disclose if it cannot be unit-tested.
- Branch: `feat/consultation-prescription-flow` (off master). One
  work-unit commit per task, Conventional Commits, no AI attribution.
- Code/comments in English; UI copy in Spanish consistent with the app.
- ~400 authored lines per task is a planning heuristic only. Forecast for the
  whole feature is ~450-550 lines; delivery strategy `ask-on-risk` (chain
  strategy will be asked once when the running count passes ~400).
- Never push, deploy, or apply migrations without explicit user approval.

## Tasks
- [x] T1 Remove the digital signature from the flow and the PDF
      (PostConsultation.tsx signature step/args, finalize-consultation payload
      and PDF, `digital_signature` stored as empty string), tests.
- [x] T2 Prescripcion field in PostConsultation (payload -> `notes`), PDF
      unchanged except no signature; tests.
- [ ] T3 WhatsApp delivery: PDF then a text message with the prescripcion
      (server/whatsapp.js + tests).
- [ ] T4 Reconnect VideoRoom "Receta" tab: pass med lines + recommendations to
      PostConsultation via navigate state and prefill (free-text lines coexist
      with catalog items in `medications`), tests.
- [ ] T5 Obra social alternative mode: mode selector, link field + validation,
      server handler that sends the link through company WhatsApp, delivery
      log decision (see Design), tests.
- [ ] T6 (user step, not code) Deploy `finalize-consultation` edge function and
      run the end-to-end WhatsApp test with the real number.

## Route declaration
T1-T5: delegated writer (2+ non-trivial files each: PostConsultation.tsx,
VideoRoom.tsx, edge function, server/whatsapp.js and their tests). One writer
at a time.

## Progress / evidence
- 2026-09-23: branch created, mapping done (see Findings).
- T1 (delegated writer, commit `85662bd`): PIN modal, crypto imports and
  signature args removed from PostConsultation; edge function no longer reads
  signature fields, PDF has no signature/authenticity footer,
  `digital_signature` stored as `''`, `signature_public_key` omitted from the
  insert (column is nullable, verified in migration 20260526000000). WhatsApp
  caption text changed from "receta firmada" to "receta". RED: `npx vitest run
  src/pages/doctor/__tests__/PostConsultation.test.tsx -t "without digital
  signature"` failed (PIN modal blocked invoke); GREEN: `npx vitest run
  src/pages/doctor` 12/12.
- T2 (same writer, commit `b163e84`): optional textarea "Prescripcion /
  indicaciones"; body field `indications` (trimmed) stored in
  `prescriptions.notes` (null when blank); PDF unchanged. RED: 2 new tests
  failed; GREEN: full `npx vitest run` 692/699, only the 7 known pre-existing
  failures (VideoRoom x3, DashboardRepository, crypto, MedicalHistory x2).
- Compatibility (writer + parent spot check): `MedicalHistory.tsx` handles an
  empty signature via its `'unsigned'` branch; `DoctorDashboard.tsx` (~L958)
  shows an empty "Firma:" (cosmetic, left alone); PrescriptionRepository
  passes null notes/empty signature through. Unused after T1: `generateKeyPair`,
  `exportPublicKey`, `encryptPrivateKey`, `decryptPrivateKey`,
  `signPrescription` in `src/utils/crypto.ts` (only tests use them; file left
  untouched).
- Gap: the Deno edge function has no test harness; its changes are covered
  only by client payload tests and diff review. Needs a manual check after the
  user deploys it (T6).
- Route: T1+T2 delegated writer (PostConsultation.tsx, its test, edge function).

## Review (RDD, 2026-09-23)
T1+T2 range (`060ceb0..HEAD`, medium risk, 521 lines incl. this doc) reviewed
with the reliability lens and approved; acknowledgement done (authority
burned). Non-blocking follow-ups (do not reopen the review):
- WARNING `finalize-consultation/index.ts:197`: `indications` is stored only
  when the prescriptions row is inserted, which happens only with medications;
  the client shows the indications field regardless of the "Receta Electronica"
  toggle, so indications written without medications are silently dropped.
  DECISION: handled inside T3 (create the row when there are medications OR
  indications; PDF only when there are medications; WhatsApp then sends the PDF
  and/or the indications text).
- SUGGESTION: no test for the edge-function changes (known gap, see T6);
  optionally move the notes rule into a small pure helper.
- SUGGESTION: the "no PIN step" test asserts absence of text matching "Firma";
  tighten it (assert no PIN/password input) when T3 touches that test file.
Delivery: running count (521) passed ~400; user chose `feature-branch-chain`
(2026-09-23): slices are PRs into the feature branch and only the final PR
goes to master, so production never deploys a half-built feature. Slice 1 =
T1+T2 (commits `85662bd`, `b163e84`, plus the tracking doc). Push and PR
creation stay the user's decision.

## Next step
T3: prescripcion-only support + second WhatsApp text message (edge function,
server/whatsapp.js + tests).
